# Fix Applied: Priority-wise Tickets Query

## Problem
Query: **"Priority wise tickets summary show karo"**
Result: **"No data found"**

## Root Cause
AI planner didn't have example for priority-wise grouping, so it might have generated wrong plan.

## Solution Applied

### 1. Added Example to AI Planner
**File:** `app/Services/AiQueryPlannerService.php`

Added example:
```php
[
    'question' => 'Priority wise tickets summary show karo',
    'expected_plan' => [
        'answer_type' => 'table',
        'intent' => 'crm_group_summary',
        'tables' => ['service_tickets'],
        'columns' => ['priority'],
        'group_by' => ['priority'],
        'filters' => [],
    ],
]
```

### 2. Generic Query Builder Already Supports GROUP BY
**File:** `app/Services/AiGenericQueryBuilderService.php`

Already has `applyGroupedSelect()` method that handles:
- GROUP BY columns
- COUNT(*) as aggregate
- Proper column selection

### 3. Expected SQL
```sql
SELECT 
    service_tickets.priority as priority,
    COUNT(*) as aggregate
FROM service_tickets
WHERE service_tickets.deleted_at IS NULL
LIMIT 100
GROUP BY service_tickets.priority
```

## Next Steps

### 1. Clear Cache
```bash
php artisan cache:clear
php artisan config:clear
composer dump-autoload
```

### 2. Run Debug Script
```bash
php debug-priority-tickets.php
```

This will show:
- How many tickets in database
- What priorities exist
- Generated plan from AI
- Generated SQL
- Query results

### 3. Test in Browser
Try these queries:
```
"Priority wise tickets summary show karo"
"Show tickets grouped by priority"
"Tickets priority wise"
"Status wise tickets"
"Department wise projects"
```

## What Was Already Done

### ✅ Generic Query Builder Created
- Handles ANY intent dynamically
- Supports GROUP BY queries
- Applies role-based filtering
- Uses Laravel Query Builder (safe!)

### ✅ Intent System Made Flexible
- No hardcoded enum
- AI can generate any intent
- Examples guide AI behavior

### ✅ Relationship Handling
- Automatic JOINs from schema
- Multi-table queries supported

## If Still Not Working

### Check 1: Database Has Tickets
```bash
php artisan tinker
>>> \App\Models\ServiceTicket::count()
```

If 0, add test data:
```php
\App\Models\ServiceTicket::create([
    'subject' => 'Test Ticket',
    'priority' => 'High',
    'status' => 'Pending',
    'user_id' => 1,
]);
```

### Check 2: Priority Column Exists
```bash
php artisan tinker
>>> \App\Models\ServiceTicket::first()->priority
```

### Check 3: Run Debug Script
```bash
php debug-priority-tickets.php
```

Look for:
- Plan generated by AI
- SQL query
- Validation status
- Execution results

### Check 4: Check Logs
```bash
tail -f storage/logs/laravel.log
```

## Summary

**Changes Made:**
1. ✅ Added priority-wise example to AI planner
2. ✅ Generic builder already supports GROUP BY
3. ✅ Created debug script

**Expected Result:**
```
Query: "Priority wise tickets summary show karo"
Result: Table showing:
| priority | aggregate |
|----------|-----------|
| High     | 5         |
| Medium   | 3         |
| Low      | 2         |
```

**Run This Now:**
```bash
php artisan cache:clear
php debug-priority-tickets.php
```
